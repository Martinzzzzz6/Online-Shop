specVersion: "v1beta"
serviceId: "sebiz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sebiz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
